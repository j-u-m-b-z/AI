name: human-image-validation-webapp
description: human image validation webapp poc
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
  aws:region: ap-southeast-1
  deploymentEnvironment: production
